#!/usr/bin/env bash
# Health check: ChromaDB  (placeholder — service still being set up)
CHROMA_URL="${CHROMA_URL:-http://localhost:8000}"

echo "SKIP  chromadb — placeholder (not yet configured, expected at $CHROMA_URL)"
exit 0
